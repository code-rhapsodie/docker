

services:
  - docker

env:
  - IMAGE=ansible VERSION=debian-buster
  - IMAGE=ansible VERSION=ezp-collections

before_script:
  - env | sort
  - cd "$IMAGE/$VERSION"
  - image="$IMAGE:$VERSION"

script:
  - docker build -t "$image" .

after_script:
  - docker images
